# Base URL of the SQLens backend (FastAPI). Also used to derive the
# WebSocket chat URL (http -> ws, https -> wss). Must be a public NEXT_PUBLIC_
# var since it's read in the browser, not just at build time.
NEXT_PUBLIC_API_URL=http://localhost:8000
